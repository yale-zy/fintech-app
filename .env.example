# Copy to .env and adjust values

# Base URL for the real API
VITE_API_BASE_URL=http://localhost:8080/api

# Mock is configured per-endpoint in src/api/mock/handlers.js (mockedEndpoints)
# Comment out or delete an entry there to use the real API for that endpoint
